# URL del backend. Si esta vacia o no existe este archivo, el sitio funciona
# igual: resuelve el catalogo contra data/catalog.json (modo demostracion).
#
# Para desarrollo local con el backend levantado:
# VITE_API_URL=http://localhost:4000
#
# En produccion apuntar al dominio donde corra la API:
# VITE_API_URL=https://api.eurocampingonline.com.ar
